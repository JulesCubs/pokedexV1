import { Text, View } from "react-native"
import { SafeAreaView } from "react-native-safe-area-context"

export const PokedexScreen = () => {
    return (
        <SafeAreaView>
            <View><Text>Pokedex</Text></View>
        </SafeAreaView>
    )
}
